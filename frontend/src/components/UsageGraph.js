import React from "react";

function UsageGraph({ data }) {
  return (
    <div>
      <h2 className="text-xl font-semibold">Water Usage Graph</h2>
      <p>Graph placeholder (use Chart.js / Plotly for real graphs)</p>
    </div>
  );
}

export default UsageGraph;
